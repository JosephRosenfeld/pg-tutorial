import pg from "pg";

const pool = new pg.Pool({
  user: "postgres",
  password: "{Put your password for your postgres acct here}",
  database: "todo_database",
  host: "localhost",
  port: 5432,
});

export default pool;